# filters-ux
1. The filters should be on the left
2. Each filter should be expandable
3. The applied filter should be shown in a bar on top of the pictures list
4. Media type filter should be checkboxes
5. Date to search - I should have 2 different filters: Imported date and Captured date. Both date ranges.
6. Tags should show the list of all tags used in order of most common. I can click on each one of them to add it to the filter and re-click to remove.
7. Instead of having the bar with 4 buttons on top (Import media, Library Settings, Close, Danger zone), let's create a top bar with icon setting button on the right with all these options.
8. remove "Browse imported copies. Originals remain untouched." and "LIBRARY SEARCH" labels
