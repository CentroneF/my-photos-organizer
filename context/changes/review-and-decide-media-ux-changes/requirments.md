# UX Changes to implement
1. Ones a library is selected, go full screen and remove the left blue column. 
2. Remove the multisteps (1 -> 2) from top of the screen
3. Remove the lock library button
4. Add button next to protected library path to folder in OS finder
5. Move the Open danger zone button, under the library path. The form should open in a new page.
6. the review picture shouldn't be bigger than the full screen.
